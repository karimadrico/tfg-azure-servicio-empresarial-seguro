\apendice{Documentación técnica de programación}

\section{Introducción}

Este anexo describe la estructura técnica del repositorio y los pasos para ejecutar, probar y desplegar la solución, apoyándose en scripts reproducibles con Azure CLI y servicios gestionados de Azure \cite{azure_keyvault,azure_appservice_keyvault_python}.

\section{Estructura de directorios}

\begin{itemize}
    \item \texttt{src/app.py}: API principal Flask.
    \item \texttt{src/classifier.py}: clasificador automático de incidencias.
    \item \texttt{src/storage.py}: persistencia local o en Azure Blob Storage.
    \item \texttt{src/config.py}: configuración por variables de entorno.
    \item \texttt{src/requirements.txt}: dependencias Python.
    \item \texttt{tests/test\_api.py}: pruebas unitarias.
    \item \texttt{logicapp/}: definición del flujo de automatización HTTP.
    \item \texttt{infra/terraform/}: infraestructura como código.
    \item \texttt{scripts/}: despliegue y verificación en Azure.
    \item \texttt{memoria/tex/}: memoria y anexos en LaTeX.
\end{itemize}

\section{Ejecución local}

\begin{verbatim}
cd src
pip install -r requirements.txt
set STORAGE_MODE=local
python app.py
\end{verbatim}

La API quedará disponible en \texttt{http://localhost:5000}.

\section{Variables de entorno}

\begin{itemize}
    \item \texttt{STORAGE\_MODE}: \texttt{local} o \texttt{azure}.
    \item \texttt{AZURE\_STORAGE\_CONNECTION\_STRING}: cadena de conexión del Storage Account.
    \item \texttt{KEY\_VAULT\_URL}: URI del Key Vault.
    \item \texttt{API\_KEY}: token Bearer opcional para desarrollo local.
\end{itemize}


\section{Infraestructura como código}

El repositorio incluye definiciones de infraestructura como código en Terraform y Bicep. En el alcance final del TFG, el despliegue operativo se realiza mediante scripts PowerShell y Azure CLI, mientras que Terraform y Bicep actúan como documentación técnica reproducible de los recursos cloud necesarios. Esta separación permite defender tanto la ejecución práctica del despliegue como el diseño declarativo de la infraestructura.

La tabla \ref{tabla:iac_comparativa} resume el papel de cada mecanismo utilizado.

\begin{table}[h]
\centering
\small
\begin{tabularx}{\textwidth}{l X X}
\textbf{Mecanismo} & \textbf{Uso en el proyecto} & \textbf{Ventaja principal} \\
\hline
PowerShell + Azure CLI & Despliegue real del App Service, configuración de variables, Key Vault y verificación. & Permite ejecutar y demostrar el despliegue desde el entorno local. \\
Terraform & Definición declarativa de Resource Group, App Service, Storage, Key Vault, RBAC y Application Insights. & Documenta la infraestructura de forma portable y auditable. \\
Bicep & Plantillas alternativas de recursos Azure, incluida la Logic App. & Expresa recursos Azure de forma nativa y revisable por SonarCloud. \\
Portal Azure & Validación visual y capturas de evidencias. & Facilita al tribunal comprobar recursos reales desplegados. \\
\end{tabularx}
\caption{Comparativa de mecanismos de infraestructura y despliegue}
\label{tabla:iac_comparativa}
\end{table}

En Terraform se han descrito los recursos principales del prototipo: grupo de recursos, plan de App Service, aplicación Linux, cuenta de almacenamiento con cifrado e identidad, contenedor privado, Key Vault con RBAC y Application Insights. Estas definiciones no sustituyen a las pruebas del despliegue real, sino que complementan la documentación técnica del sistema y reflejan cómo podría evolucionar hacia un proceso IaC completo.
\section{Despliegue en Azure}

\begin{enumerate}
    \item Iniciar sesión con \texttt{az login}.
    \item Definir la variable de entorno \texttt{API\_KEY} con el token que se almacenará en Key Vault.
    \item Ejecutar \texttt{scripts/deploy-azure.ps1} desde la raíz del repositorio.
    \item Comprobar \texttt{/health} y abrir el portal web desplegado.
    \item Ejecutar \texttt{scripts/verify-azure.ps1} para verificar endpoints principales.
    \item Ejecutar \texttt{scripts/deploy-logicapp.ps1} para publicar el flujo HTTP de automatización cuando se desee probar la integración externa.
\end{enumerate}

La figura \ref{fig:despliegue-azure} resume el proceso de despliegue y verificación ejecutado sobre Azure.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{despliegue_azure.png}
\caption{Proceso de despliegue y verificación en Azure. Fuente: elaboración propia.}
\label{fig:despliegue-azure}
\end{figure}

\section{Pruebas del sistema}

\begin{verbatim}
python -m unittest discover -s tests -p "test_*.py"
\end{verbatim}

La batería contiene veintiuna pruebas que verifican endpoints, autenticación, creación, consulta y actualización de solicitudes, persistencia, historial, transiciones inválidas, filtros, paginación, portal, clasificación y métricas con SLA. También cubre catálogo de servicios, cálculo de impacto, aprobación, escalado, centro operativo, exportación CSV y refresco de Blob Storage entre procesos de App Service.

La tabla \ref{tabla:pruebas_realizadas} resume las pruebas principales aplicadas durante la validación del prototipo.

\begin{table}[h]
\centering
\small
\begin{tabularx}{\textwidth}{l X X}
\textbf{Prueba} & \textbf{Objetivo} & \textbf{Resultado esperado} \\
\hline
Endpoint raíz & Comprobar disponibilidad básica del servicio. & Respuesta 200 con estado operacional. \\
Creación de solicitud & Validar entrada, clasificación y persistencia. & Respuesta 201 con identificador, prioridad y recomendación. \\
Portal web & Verificar acceso a la interfaz de usuario. & HTML del portal cargado correctamente. \\
Métricas & Comprobar agregación de solicitudes. & Totales por estado, prioridad y tipo. \\
Gestión de solicitud & Validar asignación, estado, prioridad e historial. & Actualización persistida y trazable. \\
Catálogo e impacto & Relacionar servicio, activo y entorno con una solicitud. & Propietario e impacto calculados de forma coherente. \\
Aprobación y escalado & Comprobar el flujo de decisiones sensibles. & Estado, aprobador, nivel de escalado e historial actualizados. \\
Centro operativo y CSV & Validar carga, alertas y exportación. & Agregados correctos e informe descargable sin fórmulas ejecutables. \\
Consistencia Azure & Simular lecturas sucesivas desde Blob Storage. & Cada proceso obtiene la versión más reciente de los datos. \\
Validaciones de gestión & Rechazar estados desconocidos y recursos inexistentes. & Respuestas 400 y 404 según el caso. \\
Clasificación de seguridad & Validar detección de incidencias críticas. & Categoría seguridad y prioridad alta. \\
Escenarios de reglas & Ejecutar cuatro entradas con coincidencias y valores de respaldo. & Tipo, categoría y prioridad coinciden con las reglas documentadas. \\
Verificación Azure & Probar despliegue real con script. & Health check, creación, listado y métricas correctas. \\
Logic App & Validar automatización externa de solicitudes. & Trigger HTTP invoca \texttt{POST /solicitudes} y devuelve una solicitud creada. \\
SonarCloud & Revisar calidad externa del código. & Quality Gate aprobado y duplicación controlada. \\
\end{tabularx}
\caption{Pruebas realizadas sobre el prototipo}
\label{tabla:pruebas_realizadas}
\end{table}
\FloatBarrier

\section{Análisis de calidad}

El proyecto utiliza SonarCloud como servicio gestionado de SonarQube:

\begin{itemize}
    \item \href{https://sonarcloud.io/project/overview?id=karimadrico_tfg-azure-servicio-empresarial-seguro}{Panel público del proyecto en SonarCloud}.
    \item \texttt{docs/calidad/sonarcloud.md}: documenta el resultado observado y las evidencias que deben guardarse.
\end{itemize}

Como evidencia se conserva una captura del análisis del 22 de junio, posterior a la ampliación funcional. El Quality Gate está aprobado y las calificaciones de seguridad, fiabilidad y mantenibilidad son A, sin issues abiertos ni hotspots pendientes. La configuración fija Python 3.11 y la versión del proveedor Azure de Terraform para evitar análisis ambiguos.


\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{sonarcloud-quality-gate-final-22junio.png}
\caption{Análisis final de SonarCloud: Quality Gate aprobado y cero issues abiertos. Fuente: captura propia.}
\label{fig:sonarcloud-quality-gate-final}
\end{figure}
La figura \ref{fig:observabilidad-monitor} resume la estrategia de observabilidad mediante App Service, Application Insights y Azure Monitor.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{observabilidad_monitor.png}
\caption{Observabilidad del prototipo con Azure Monitor y Application Insights. Fuente: elaboración propia.}
\label{fig:observabilidad-monitor}
\end{figure}
Las figuras \ref{fig:application-insights-overview} y \ref{fig:azure-monitor-metrics} muestran evidencias del recurso de Application Insights y de las métricas disponibles para supervisar el servicio desplegado.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{../docs/evidencias/application-insights-overview.png}
\caption{Vista general de Application Insights asociado al prototipo. Fuente: captura propia.}
\label{fig:application-insights-overview}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{../docs/evidencias/azure-monitor-metrics.png}
\caption{Métricas del servicio en Azure Monitor. Fuente: captura propia.}
\label{fig:azure-monitor-metrics}
\end{figure}
\FloatBarrier
\section{Mantenimiento operativo}

El mantenimiento del prototipo se organiza alrededor de los siguientes puntos:

\begin{itemize}
    \item Revisar el endpoint \texttt{/health} tras cada despliegue.
    \item Ejecutar \texttt{scripts/verify-azure.ps1} para comprobar portal, API, creación de solicitudes, listado y métricas.
    \item Mantener la clave de API en Azure Key Vault, evitando valores embebidos en el repositorio.
    \item Revisar Application Insights y Azure Monitor para detectar errores, tiempos de respuesta o ausencia de peticiones.
    \item Revisar SonarCloud tras cambios relevantes para mantener el Quality Gate aprobado.
    \item Documentar evidencias finales en \texttt{docs/evidencias/} antes de generar los entregables.
\end{itemize}


